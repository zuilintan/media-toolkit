"""
workflow — 高层工作流（目录扫描、批量执行、ComicInfo 写入、回退）

子模块:
    rename_kit  — 源文件扫描 & 重命名执行（rename-kit 子命令）
    meta_kit  — ComicInfo.xml 生成 & 写入（meta-kit 子命令）
    cover_kit — CBZ 封面生成 & 写入（cover-kit 子命令）
    pack_kit  — 图片目录序号化重命名 + STORED zip 打包（pack-kit 子命令）
"""
