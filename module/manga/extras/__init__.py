"""
manga.extras — 非 *-kit 业务的辅助模块集合

    doctor.py    — manga-cli doctor 子命令（依赖体检）
    examples.py  — rename-kit / meta-kit 的 --examples 选项共用的示例演示运行器

与 cli/<subcmd>_kit.py 区分: extras 内是诊断 / 演示等旁路功能，
不属于 manga 的核心业务流程。
"""
