"""artifact-toolkit 业务包（以"文件搬移/整理"为核心）。

对标 :mod:`module.manga` 的结构：cli / gui / workflow 三层划分；
首个业务模块为 :mod:`~module.artifact.workflow.classify`（文件归类）。
"""

from base import __version__  # noqa: F401
